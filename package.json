{
  "name": "cds-agent-libs",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for cds-agents — AI agent integration for SAP CAP",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "keywords": ["sap", "cap", "cds", "ai", "agents", "langchain"],
  "license": "MIT"
}
